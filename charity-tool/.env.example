# Get a free key: register at https://api-portal.charitycommission.gov.uk/
# then subscribe to the "Register of Charities" product and copy your key here.
CHARITY_COMMISSION_API_KEY=your-subscription-key-here

# Optional: how long (seconds) to cache API responses. Default 86400 (24h).
CACHE_TTL_SECONDS=86400
